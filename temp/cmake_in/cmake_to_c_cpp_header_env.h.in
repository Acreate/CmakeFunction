#ifndef CMAKE_TO_C_CPP_HEADER_ENV_H_H_HEAD__FILE__
#define CMAKE_TO_C_CPP_HEADER_ENV_H_H_HEAD__FILE__
#pragma once

#define qt_version "${QT_VERSION}" // qt 版本
#define Builder_Tools_Clang ${Clang} // 使用 clang 编译器
#define Builder_Tools_GNU ${GNU} // 使用 gnu 编译器
#define Builder_Tools_MSVC ${MSVC} // 使用 msvc 编译器

#define Cmake_Project_Name "${PROJECT_NAME}" // cmake 项目名称
#define Project_Run_bin "${Project_Run_bin}" // cmake 运行目录
#define Project_Plug_bin "${Project_Plug_bin}" // cmake 插件目录
#define Cmake_Source_Dir "${CMAKE_SOURCE_DIR}" // cmake 根目录
#define Archive_Output_Name "${ARCHIVE_OUTPUT_NAME}" // cmake 输出名称
#define Compile_Pdb_Name "${COMPILE_PDB_NAME}" // cmake pbd 名称

#define Is_Dll "${IsDLL}"


#define COMPILE_OPTIONS "${COMPILE_OPTIONS}" 
#define COMPILE_FLAGS "${COMPILE_FLAGS}" 
#define COMPILE_DEFINITIONS "${COMPILE_DEFINITIONS}" 
#define ANDROID_PROGUARD_CONFIG_PATH "${ANDROID_PROGUARD_CONFIG_PATH}" 
#define ANDROID_SECURE_PROPS_PATH "${ANDROID_SECURE_PROPS_PATH}" 
#define AUTOMOC_PATH_PREFIX "${AUTOMOC_PATH_PREFIX}" 
#define EXPORT_NAME "${EXPORT_NAME}" 
#define IMPORTED_IMPLIB "${IMPORTED_IMPLIB}" 
#define AUTOMOC_MACRO_NAMES "${AUTOMOC_MACRO_NAMES}" 
#define BUILD_WITH_INSTALL_NAME_DIR "${BUILD_WITH_INSTALL_NAME_DIR}" 
#define C_EXTENSIONS "${C_EXTENSIONS}" 
#define C_STANDARD "${C_STANDARD}" 
#define CXX_EXTENSIONS "${CXX_EXTENSIONS}" 
#define CXX_STANDARD "${CXX_STANDARD}" 
#define DLL_NAME_WITH_SOVERSION "${DLL_NAME_WITH_SOVERSION}" 
#define INSTALL_NAME_DIR "${INSTALL_NAME_DIR}" 
#define LIBRARY_OUTPUT_NAME "${LIBRARY_OUTPUT_NAME}" 
#define LIBRARY_OUTPUT_DIRECTORY "${LIBRARY_OUTPUT_DIRECTORY}" 
#define LINK_FLAGS "${LINK_FLAGS}" 
#define LINK_DEPENDS "${LINK_DEPENDS}" 
#define LINK_DIRECTORIES "${LINK_DIRECTORIES}" 
#define LINK_LIBRARIES "${LINK_LIBRARIES}" 
#define LINK_OPTIONS "${LINK_OPTIONS}" 
#define LINKER_TYPE "${LINKER_TYPE}" 
#define LINKER_LANGUAGE "${LINKER_LANGUAGE}" 
#define NAME "${NAME}" 
#define MSVC_RUNTIME_LIBRARY "${MSVC_RUNTIME_LIBRARY}" 
#define MSVC_DEBUG_INFORMATION_FORMAT "${MSVC_DEBUG_INFORMATION_FORMAT}" 
#define OUTPUT_NAME "${OUTPUT_NAME}" 
#define PREFIX "${PREFIX}" 
#define PRIVATE_HEADER "${PRIVATE_HEADER}" 
#define PROJECT_LABEL "${PROJECT_LABEL}" 
#define RESOURCE "${RESOURCE}" 
#define RUNTIME_OUTPUT_NAME "${RUNTIME_OUTPUT_NAME}" 
#define SYSTEM "${SYSTEM}" 
#define UNITY_BUILD "${UNITY_BUILD}" 
#define TYPE "${TYPE}" 
#define VERSION "${VERSION}" 
#endif // CMAKE_TO_C_CPP_HEADER_ENV_H_H_HEAD__FILE__
